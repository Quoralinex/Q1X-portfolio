export const meta = () => [{ title: 'Health Check' }];

export default function HealthRoute() {
  return (
    <main
      style={{
        fontFamily: 'system-ui, -apple-system, BlinkMacSystemFont, sans-serif',
        fontSize: '14px',
        padding: '1rem',
      }}
    >
      OK - q1x-portfolio
    </main>
  );
}
